# T-003 mutation proof — binder-scroll-restore-smoke can fail

**When:** 2026-07-31  
**Agent:** buffalo  
**Branch tip under test:** `5820da2` (measurement land: dual-width + STORYLINT_HEAD)  
**Product under mutation:** `src/components/shell/Binder.tsx` (throwaway; restored after)

## Why

A green smoke cannot distinguish "product restore works" from "smoke does not measure restore."
Standing rule: a check that has never been shown red is not proven capable of catching the defect.

## Mutation applied (throwaway)

In `Binder.tsx`, all three restore sites forced `scrollTop = 0` instead of the saved top:

1. F1 `useLayoutEffect` on detail close: `body.scrollTop = 0` (was `listScrollTopRef.current`)
2. focus-restore microtask: `body.scrollTop = 0` (was `savedTop`)
3. double-rAF re-assert: `body.scrollTop = 0` (was `savedTop`)

`void savedTop` added only so tsc would build with unused `savedTop`. File restored from HEAD after the run. **No product change shipped.**

## Command

```
node e2e/binder-scroll-restore-smoke.mjs
```

Owned-stack build @ `5820da2`.

## Transcript (mutated product)

```
[owned-stack] building ... @ 5820da2
[owned-stack] api :55523 ui :55524
{
  "head": "5820da2",
  "cases": [
    {
      "label": "rail@1440",
      "preOpen": 997,
      "after": { "scrollTop": 0, "editor": false, "focusName": "Char 300" },
      "delta": -997,
      "ok": false
    },
    {
      "label": "drawer@390",
      "preOpen": 1005,
      "after": { "scrollTop": 0, "editor": false, "focusName": "Char 300" },
      "delta": -1005,
      "ok": false
    }
  ]
}
FAIL scroll restore
```

## Control (unmutated product, same smoke)

```
head: a0eb622 / later 5820da2 stack
rail@1440 delta 0 ok true
drawer@390 delta 0 ok true
PASS scroll restore
```

## Verdict

Smoke **discriminates**. Disable restore → red with large negative deltas at both AZ widths.
Enable restore → green delta 0. Product STALE classification for list-click Back is measurement, not inference.

## Not claimed by this proof

- Non-list arrival (graph node / Inbox / `requestedSheetId`) — separate probe
- L2 composition on density story pool
- That every historical AZ hold sample was product-red (see ticket: first AZ sample was also probe-false-red before honesty fix)
