---
id: T-003
title: Binder list scroll restore after detail Back (AZ hold)
priority: P1
status: in_review
story: density-milestone
owner: buffalo
branch: storylint/t-003-binder-scroll
origin_dev_at_open: 2e2561d
acceptance: After opening a binder detail and Back, list scroll position restores; probe samples after place-in-fold; null = NOT-MEASURED not HIGH
shortest_repro: docs/decisions/canon-under-load-az.md; e2e binder scroll-restore sample @68 (honest probe already on dev — product restore still open)
l1_proof: owned-stack binder scroll check (rail@1440 + drawer@390); head required
l2_required: yes
blocks: []
blocked_by: []
files:
  - e2e/binder-scroll-restore-smoke.mjs
  - e2e/all-smoke.mjs
decision: docs/decisions/canon-under-load-az.md
notes: |
  Classification (buffalo T-003 investigation on origin/dev after ce1b3c7 sprint model):
  **Product restore is NOT open.** octopus fix (bfdc4d8) is correct. Owned-stack drive:
  rail@1440 delta=0; drawer@390 delta=0; head cited. Ticket text was stale relative to product.
  Remaining gap was measurement: under all-smoke the smoke printed head=unknown because
  STORYLINT_HEAD was not passed (standing rule 1). Smoke also only ran @1440 while AZ required
  both rail@1440 and drawer@390. This land closes measurement, not product code.
---

## Context

AZ required binder list scroll restore after detail Back on rail@1440 and drawer@390.
Product restore landed earlier (`fix(binder): scroll restore wins over focus`). An honest probe
false-red nearly caused a revert. T-003 stayed open describing a product hold that no longer
matches the running product.

## Classification

| Possibility | Verdict |
|---|---|
| 1. Ticket stale — defect gone | **Yes for product** — restore works @1440 and @390 |
| 2. Different case still broken | **No** — drawer@390 measured delta=0 on owned stack |
| 3. Smoke does not measure restore | **Partially** — smoke did measure delta, but printed `head=unknown` under all-smoke and omitted @390 |

## Acceptance detail

- [x] User-visible scroll restore on Back (product already on tip; re-driven)
- [x] L1 owned-stack proof without condition-label-only evidence (dual-width smoke + head required)
- [ ] L2 on density story pool (story gate; not this land alone)

## Evidence (pre-land)

Owned-stack probe head=`a0eb622` era tip parent; dual-width PASS delta=0 both surfaces.
Standalone smoke after change must print real head and both case labels.
