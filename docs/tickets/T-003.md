---
id: T-003
title: Binder list scroll restore after detail Back (AZ hold)
priority: P1
status: in_review
story: density-milestone
owner: buffalo
branch: storylint/t-003-binder-scroll
origin_dev_at_open: 2e2561d
acceptance: After opening a binder detail and Back, list scroll position restores; probe samples after place-in-fold; null = NOT-MEASURED not HIGH
shortest_repro: docs/decisions/canon-under-load-az.md; e2e binder scroll-restore sample @68 (honest probe already on dev — product restore still open)
l1_proof: owned-stack binder scroll check (rail@1440 + drawer@390); head required; mutation red proven
l2_required: yes
blocks: []
blocked_by: []
files:
  - e2e/binder-scroll-restore-smoke.mjs
  - e2e/all-smoke.mjs
  - e2e/proofs/T-003-scroll-restore-mutation.txt
decision: docs/decisions/canon-under-load-az.md
notes: |
  Classification (buffalo T-003):
  **Product list-click restore is STALE** — octopus fix bfdc4d8 is correct.
  Mutation proof (e2e/proofs/T-003-scroll-restore-mutation.txt): force scrollTop=0 →
  rail@1440 delta=-997, drawer@390 delta=-1005, FAIL. Unmutated → delta=0 both.
  Smoke discriminates; STALE is measurement not inference.
  This land is measurement only: dual-width + STORYLINT_HEAD under all-smoke.
  AZ hold provenance: ox ruling canon-under-load-az.md §1 (preOpen 2080 → restored 1147
  delta -933 @1440). Product fix bfdc4d8; later a7b8037 showed the original AZ sample
  was also a false-red baseline (sampled before place-in-fold). Ticket stayed open past both.
  Non-list arrival (graph/Inbox) tracked separately if broken — new ticket, not reopen.
---

## Context

AZ required binder list scroll restore after detail Back on rail@1440 and drawer@390.
Product restore landed in `bfdc4d8` (`fix(binder): scroll restore wins over focus`).
Honest probe work (`a7b8037`) fixed a false-red sample path. T-003 stayed open as if
product were still held.

## AZ hold provenance (known)

| Source | What it said |
|---|---|
| `docs/decisions/canon-under-load-az.md` §1 | P0 product bug: clean Back loses list position @1440 (2080→1147, δ−933); required both rail@1440 and drawer@390 |
| `bfdc4d8` | Product fix: focus `preventScroll` + re-assert savedTop after microtask/rAF |
| `a7b8037` | Probe honesty: prior AZ sample baseline was wrong (scrollIntoViewIfNeeded before sample) |

The hold referred to **list scroll loss on clean Back after list-open detail**, measured under load @68 sheets. Not an unknown-hold. Residual ticket debt was measurement (head provenance + dual-width + proven failure mode), not an unreproduced product case on the list path.

## Classification

| Possibility | Verdict |
|---|---|
| 1. Ticket stale — defect gone | **Yes for list-click product** — restore works; mutation proves smoke can fail |
| 2. Different case still broken | **No on graph path** — graph-node and graph-other-sheet-while-scrolled delta 0; Inbox not driven |
| 3. Smoke does not measure restore | **Closed** — mutation red at both widths; head required under all-smoke |

## Acceptance detail

- [x] User-visible scroll restore on Back (list path; product on tip; re-driven + mutation)
- [x] L1 owned-stack proof without condition-label-only evidence (dual-width + head + mutation artifact)
- [ ] L2 on density story pool (story gate; not this land alone)
- [x] Non-list arrival (graph node same + other sheet while scrolled): delta 0 @83eb2cb

## Evidence

- Control PASS: rail@1440 δ0, drawer@390 δ0
- Mutation FAIL: rail@1440 δ−997, drawer@390 δ−1005 — `e2e/proofs/T-003-scroll-restore-mutation.txt`
- `all-smoke` passes `STORYLINT_HEAD` into children

## Arrival paths (post-mutation)

Owned-stack head=`83eb2cb` throwaway `e2e/_t003_arrival_probe.mjs` (not shipped):

| path | pre | after | delta | ok |
|---|---:|---:|---:|---|
| list-click | 997 | 997 | 0 | yes |
| graph-node (Char 30) | 997 | 997 | 0 | yes |
| graph-other-sheet-while-scrolled (Char 05) | 997 | 997 | 0 | yes |

Inbox deep-link not separately instrumented; product uses the same `requestedSheetId`/`openCanonSheet` door as graph.
