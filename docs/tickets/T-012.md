---
id: T-012
title: Canon shows two equal primary "New sheet" buttons in one view
priority: P2
status: done
story: none
owner: dromedary
branch: storylint/t012-t013-canon-gates
origin_dev_at_open: 077b5db
origin_dev_landed: PENDING
integrated_on: PENDING
landed_at: 2026-08-01
acceptance: One primary "New sheet" affordance in Canon at a time; the other is quiet/secondary. An author scanning Canon empty state sees one obvious next action, not two competing ones.
shortest_repro: Open Canon mode with zero sheets. Binder CANON section and the centered empty-state both render an identically-styled primary "New sheet" button simultaneously.
l1_proof: product drive on owned stack — empty Canon map CTA primary, binder New sheet ghost; populated binder primary, no map empty CTA. Code already on origin/dev (dc059e6 / Binder.tsx ghost when sheets.length===0).
l2_required: no
blocks: []
blocked_by: []
files: [src/components/shell/Binder.tsx, src/features/graph/RelationshipGraph.tsx]
decision: |
  ALREADY SATISFIED on origin/dev before this ticket land (no product code change for T-012).
  Rule applied (ox one-primary-door-per-job): **context owns the primary**.
  - True-empty Canon: **map empty CTA** is the solid primary (wayfinding / empty-P0 fold job).
  - Binder "New sheet" demotes to **ghost** while sheets.length===0; restores **primary** once
    the list is the navigator (sheets exist). Durable location wins after first sheet.
  Why map wins empty: eye is on the fold; empty-P0 exists so the map teaches the room.
  Why binder stays reachable: same handler, quiet weight — second door without second solid.
  Verified still true at base af8c0a3 (Binder.tsx:382 ghost; RelationshipGraph empty CTA primary).
notes: |
  From dolphin's UI/UX review at 6e7926f. Finding was real then; fix landed as
  storylint/b6-primary-per-job (dc059e6) before T-012 was filed. Ticket-close-only after
  product drive confirmed no dual primary remains. Do not invent a change to justify the ticket.
---

## Context

Canon once rendered two identically-weighted primary buttons for the same verb: binder CANON
section and centered empty-state CTA. ox one-primary-per-job settled which surface owns primary
by state. Product already implements that.

## Acceptance detail

- [x] Exactly one primary "New sheet" in true-empty Canon (map CTA)
- [x] Binder demoted to ghost while empty; still reachable
- [x] Rule stated in decision (map owns empty wayfinding; binder owns populated navigator)
- [x] Non-empty Canon: binder primary; map empty CTA gone with empty state

## Repro

1. Canon mode, zero sheets, desk width
2. Map empty CTA = primary New sheet; binder New sheet = ghost
3. After ≥1 sheet: binder New sheet = primary; no dual empty CTA
