---
id: T-013
title: Canon offers "Propose new edge" with zero nodes to connect
priority: P2
status: in_review
story: none
owner: dromedary
branch: storylint/t012-t013-canon-gates
origin_dev_at_open: 077b5db
acceptance: "Propose new edge" disclosure stays (ox); below 2 sheets its body is honest (hint only, no dead From/To form); at 2+ sheets fields + Send return. An author cannot fill a form whose action is impossible.
shortest_repro: Open Canon mode with zero sheets. Open "Propose new edge". Body shows empty From/To selects + fields + Send omitted — dead form chrome (pre-fix). After fix: disclosure stays, body is one status hint.
l1_proof: owned-stack product drive (0-sheet body = hint only; 2-sheet fields+Send back); domain self-edge reject cited http.ts
l2_required: no
blocks: []
blocked_by: []
files: [src/features/graph/RelationshipGraph.tsx]
decision: |
  ox empty-canon-send-proposal-weight wins over the original ticket wording: do NOT hide
  the propose disclosure on empty. Residual was dishonest body (empty selects), not the
  summary chrome. Threshold stays 2 — server rejects self-edges (distinct endpoints).
  Continuity shape: precondition false → no runnable primary / no dead form guts; honest status.
notes: |
  From dolphin's end-to-end UI/UX review at 6e7926f. Original acceptance said hide/disable
  the whole control; rat corrected after coder found ox binding memo. Residual narrowed to
  body honesty below 2 sheets.
---

## Context

The "Propose new edge" disclosure sits below the empty graph. An edge needs two distinct
endpoints (domain: `fromSheetId !== toSheetId`). Solid Send was already omitted below 2 sheets;
the residual was painting From/To/fields over empty selects.

ox `empty-canon-send-proposal-weight` keeps the disclosure as chrome and forbids removing it.
This ticket makes the **body** honest, not the summary.

## Acceptance detail

- [x] Domain check: self-edge not meaningful — server rejects `fromSheetId === toSheetId`
- [x] Disclosure remains below 2 sheets (ox non-goal honored)
- [x] Below 2 sheets: no From/To/relationship/statement fields; status hint alone
- [x] At 2+ sheets: fields + primary Send return (gate can fail both ways)
- [x] No disabled-primary costume; Send not renamed

## Repro

1. Canon mode, zero sheets
2. Open "Propose new edge"
3. Before: empty selects + fields. After: one status line only
4. Add two sheets → fields and Send proposal return
