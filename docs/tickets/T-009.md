---
id: T-009
title: Standing rule 11a (no EPERM retry) has no check — a sleep-then-retry passes the whole suite
priority: P2
status: open
story: none
owner: unassigned
branch: —
origin_dev_at_open: df405c8
acceptance: A retry inserted into saveDirect's catch fails a test; the test catches the retry by its observable effect, not by its spelling
shortest_repro: insert a 50ms sleep-then-retry into saveDirect's catch → npm test 224/224 green
l1_proof: e2e/proofs/t009-rule11a-no-check/probe.mjs — fail rename ONCE then succeed; no-retry code = 1 attempt + reject; retry code = 2 attempts + resolve + wrong bytes on disk
l2_required: no
blocks: []
blocked_by: []
files: src/server/store.ts, src/server/server.test.ts
decision: —
notes: |
  Filed by falcon at rat's request, out of T-005 review. This is the finding rat said it would
  keep if it could keep only one.

  MEASURED at f1d4a96 and re-measured at df405c8: inserting a 50ms sleep-then-retry into
  saveDirect's catch leaves the suite fully green — 186/186 then, 224/224 now. octopus's MUT-6
  test does NOT catch it, and the reason is structural rather than an oversight: that test holds
  an open handle for the whole call, so every retry also fails and the save still rejects. It
  proves "does not swallow". It cannot see "retried first".

  SO: the one standing rule whose violation would look EXACTLY like the defect being fixed is
  enforced by review alone. The person most likely to violate it is the next agent facing a flaky
  Windows box — under pressure, with a plausible motive, and nothing to stop them. A retry makes
  the symptom disappear, which reads as success.

  MECHANISM CHOSEN — behavioural, not a source scan. rat named three options; falcon built and
  measured the first before recommending it, because "did this code retry" being unobservable was
  the stated reason to doubt it. It is observable, via the transition rather than the state:

    fail rename ONCE, then let it succeed.
    no-retry code -> 1 attempt, save REJECTS, dest unchanged.
    retry code    -> 2 attempts, save RESOLVES, 'must-not-land' ON DISK.

  Measured both ways on the real ProjectStore. The seam is `mock.module('node:fs/promises')`
  under `--experimental-test-module-mocks`, available on this Node (v26.5.0). The assertion is on
  the observable effect — the save resolved and the write landed — so it is indifferent to how the
  retry is spelled, whether it is named `retry`, hidden in a helper, or inlined.

  WHY NOT A SOURCE TRIPWIRE: a scan for setTimeout/retry in store.ts is the tripwire class badger
  got caught by twice today. It cannot distinguish live code from a comment (this ticket's own
  text would trip it), and renaming the helper evades it. If one is added anyway it must be
  LABELLED a tripwire per badger's three-layer pattern and must not be the only layer.

  WHY NOT "document that it is review-enforced": that was the honest fallback if the behavioural
  test proved unbuildable. It is buildable and cheap, so the fallback is not needed.

  SCOPE NOTE: the new test requires `--experimental-test-module-mocks` in the `test` script.
  That flag change is part of this ticket and should be called out in review, since it affects
  every test file, not just this one.

---

## Context

Standing rule 11a: no EPERM retry — hiding the symptom is not a fix. It exists because a retry
turns a data-loss defect into an intermittent one, which is strictly harder to diagnose and reads
as a fix in every dashboard.

T-005 now has a real test that a failed rename does not silently succeed. It does not have, and
by its construction cannot have, a test that the failure was not retried first.

## Acceptance detail

- [ ] A test fails a rename ONCE and asserts the save rejects after exactly one attempt
- [ ] The test asserts on observable effect (rejection + dest bytes unchanged), NOT on source text
- [ ] Verified by mutation: adding a retry to saveDirect's catch makes this test fail, and the
      test passes again when the retry is removed
- [ ] If a source tripwire is added as a second layer, it is labelled a tripwire and states that
      it cannot see a differently-named helper
- [ ] `--experimental-test-module-mocks` added to the `test` script, with its blast radius noted

## Repro

1. Insert into `saveDirect`'s catch, before `throw error`:
   `if (error.code === 'EPERM') { await sleep(50); try { await rename(temporary, path); return } catch {} }`
2. `npm test` → fully green (224/224 measured at df405c8)
3. Run the once-fail seam: retry code shows 2 attempts, resolves, and leaves `must-not-land` on
   disk; restored code shows 1 attempt and rejects
