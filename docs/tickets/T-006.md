---
id: T-006
title: Silent calm death after b6 self-test (no stack, exit 1)
priority: P2
status: open
story: none
owner: unassigned
branch: storylint/t006-instrument-done
origin_dev_at_open: efdb2ca
acceptance: Next B2-class silent post-b6 exit-1 under land or capture-test-green-cause records cause lines; reader classifies via the cause→hypothesis map in this ticket (exited-1 vs terminated). Instrument is DONE in both paths — only the live hit remains.
shortest_repro: rare — once in a day of land baselines; specimen e2e/proofs/land-gate/buffalo-calm-incomplete.output cuts after [b6-primary-visibility-self-test] ok with no Error/FINGERPRINT/run-end/hard-timeout
l1_proof: node scripts/capture-test-green-cause.mjs --n 1; land baseline runCapture [cause] lines (land.mjs @ 17c72df+)
l2_required: no
blocks: []
blocked_by: []
files: [scripts/capture-test-green-cause.mjs, scripts/land.mjs, e2e/calm-budget.mjs, e2e/owned-stack.mjs]
decision: passive watch — do not re-hunt; wait instrumented hit
notes: |
  Horse 2026-08-01 investigation (report-only). DO NOT merge with T-005.
  T-005 = EPERM rename with stack present (B1). This ticket = silent incomplete (B2).
  Badger land-inside-calm EPERM capture KEPT the stack — lost-stderr is dead for that path.
  Quiet N=5 calm-alone and N=3 full test:green under piped parent: all green; B2 unreproduced.
  Seven mechanisms eliminated (see body). Instrument DONE both paths (see Instrument status). Live hit outstanding only.
  SALVAGE 2026-08-01 (e2e/proofs/calm-crash/): serial loop @ ea0500a run-08 is B1-class —
  full EPERM Error stack after both self-tests, tip pre-T-005. Classifier label
  died-after-self-tests-no-identity means no HARD/FINGERPRINT, NOT "no stack".
  DO NOT fold into T-008: loop is serial (not multi-process writers); run-08 has
  stack so map says Not B2. T-006 stays open passive watch for true silent B2.
---

## Status: passive watch

Instrument is **done** in both capture paths. This ticket is no longer an open investigation to re-run. It is a **passive watch** for the next B2-class specimen that already carries `[cause]` lines. Do not re-open the eliminated list. Do not force concurrency. Attach the log and classify with the map below.

## Instrument status (DONE)

| Path | SHA / tip | What it emits |
|------|-----------|---------------|
| Standalone N-loop tool | `ae194ed` (`scripts/capture-test-green-cause.mjs`) | `[cause] spawn/exit/close` on full `test:green` without land |
| Land baseline `runCapture` | `17c72df` (`scripts/land.mjs`, badger) | same fields on every land-captured child; rides stderr next to product output |

Fields (both paths): `pid`, `code`, `signal`, `elapsed_ms`, `first=exit|close`, and on close `exit_to_close_ms`. Comment in land.mjs: distinguishes exited-N from terminated-by-signal.

## Context

Buffalo land baseline specimen `e2e/proofs/land-gate/buffalo-calm-incomplete.output` (bi-settled @ 81dc752, ports 62314/62315):

- Ends immediately after `[b6-primary-visibility-self-test] ok`
- Zero `EPERM`, product `Error` stack, `FINGERPRINT`, `[run-end]`, `hard-timeout`
- Land then saw `opaque:test-green-exit-1` (pre stage-died:calm classifier on that tip)

Separate from T-005 / octopus path-lock: badger C1 land capture proves EPERM prints a full stack under the same harness class (`ensureDraftReady` rename). B2 has no such text.

## Eliminated mechanisms (do not re-run)

| # | Mechanism | Why dead |
|---|-----------|----------|
| 1 | Lost stderr / capture race | `scripts/land.mjs` runCapture pipes stdout+stderr and resolves on `'close'` not `'exit'`. B1 kept full EPERM stack under land. |
| 2 | Bare `process.exit(1)` truncating writes | calm-budget: only exit(1) is post-FINGERPRINT HARD path; refuses are exit(2)+REFUSE. owned-stack: no `process.exit`. helpers: exit(124) hard-timeout only. |
| 3 | hard-timeout | Prints `FAIL … hard-timeout`, exit **124**, not 1. |
| 4 | EPERM-as-silent (same as T-005) | B1 has EPERM+stack; B2 has zero EPERM text. Do not absorb into T-005 without EPERM in the specimen. |
| 5 | killTree upward self-kill of calm | `taskkill /PID apiChild.pid /T /F` kills tree **rooted at** API child only (downward). apiChild is calm's direct `spawn(process.execPath, …)` child. Parent calm is not in that tree. |
| 6 | Pipe alone forces death | N=5 quiet `npm run calm` + N=3 full `test:green` as piped child via `capture-test-green-cause.mjs`: all green, signal=null, first=exit. |
| 7 | Prior stages alone force death | Full chain (guard+build+unit+10 smokes+calm) under pipe: 3/3 green ~71–78s, FINGERPRINT eddd76a4. |

## Cause line → hypothesis map (read this on the next hit)

On a B2-class log (cuts after b6-ok, exit 1, no stack), read the trailing `[cause] exit` / `[cause] close` lines and classify:

| Observed cause shape | Classification | What it kills / keeps |
|---------------------|----------------|------------------------|
| `signal=SIGTERM` (or any non-null signal), any code | **Terminated** — external or parent kill | Confirms external-kill class. All "calm exited by itself" theories secondary. |
| `signal=SIGKILL` / signal non-null, short elapsed | **Hard kill** | Same class; not a JS throw path. |
| `code=1 signal=null first=exit` | **Self-exit 1** — process exited without signal | Every external-killer theory is dead for that specimen (including prior rat hypotheses). Remaining: an unnamed path that returns 1 with no further stdout after b6-ok. |
| `code=1 signal=null first=close` (exit missing or late) | **Close-without-clean-exit ordering** | Capture still got pipes closed; check `exit_to_close_ms` and whether exit line exists at all. |
| `code=null signal=null` | **Abnormal handle loss** | Treat as terminate-class until proven otherwise; attach full log. |
| `code=124` or hard-timeout text | **Not B2** | That is mechanism #3 (eliminated as B2). File elsewhere if needed. |
| EPERM / Error stack present | **Not B2** | That is T-005 / B1. Do not absorb here. |

One live hit with cause lines ends the ambiguity that blocked eight hypotheses. No further pre-hit investigation.

## Still open (unproven — wait for hit)

- External kill of the calm / npm.cmd pid while land (or concurrent agents) run
- Tip-specific or environment-specific rare fault not present on current tips under quiet load
- Any path that returns 1 with no further stdout/stderr after b6-ok (none named in calm/owned-stack greps)

**Not chased:** forcing concurrency. Next live hit under cause instrumentation answers exited-1 vs terminated without another hypothesis round.

## Acceptance detail

- [x] Standalone cause capture tool: `scripts/capture-test-green-cause.mjs` @ `ae194ed`
- [x] Same cause fields in `scripts/land.mjs` runCapture @ `17c72df` (badger)
- [ ] One live B2-class occurrence with cause lines showing code vs signal
- [ ] Named mechanism or wontfix with evidence after instrumented hit (use map above)

## Repro (when it happens)

1. Prefer land baseline (cause lines automatic @ 17c72df+) or `node scripts/capture-test-green-cause.mjs --n 1 --label …`
2. If log ends after b6 self-test with exit 1 and no stack: read trailing `[cause] exit` / `[cause] close` lines
3. Classify with the map in this ticket; attach log; do not re-run the eliminated list

## Tool

```text
node scripts/capture-test-green-cause.mjs
node scripts/capture-test-green-cause.mjs --n 3 --label b2-full-chain
```

Emits into capture buffer and `_land_run/<label>-N.log`:

```text
[cause] spawn pid=… shell=… cmd=…
[cause] exit  pid=… code=… signal=… elapsed_ms=… first=exit|close
[cause] close pid=… code=… signal=… elapsed_ms=… first=… exit_to_close_ms=…
```

Land path: same line shape from `runCapture` on every captured child (no extra flags).
