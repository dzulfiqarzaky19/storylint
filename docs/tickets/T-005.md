---
id: T-005
title: Project atomic rename EPERM can fail saves on Windows
priority: P1
status: in_review
story: none
owner: octopus
branch: storylint/t005-path-lock
origin_dev_at_open: ea0500a
acceptance: Failed atomic project write (rename .json.tmp → .json) surfaces to the author; no silent loss; rename-fail path has a failing test
shortest_repro: calm-budget isolated loop run-8 @ ea0500a — ensureDraftReady create 400 EPERM rename under data/projects
l1_proof: e2e/proofs/t005-eperm-path-lock/ (BEFORE 988 EPERM thrash → AFTER 0; load-unlock mutation 11 EPERM); server.test multi-instance + concurrent list+PUT
l2_required: no
blocks: []
blocked_by: []
files: src/server/store.ts, src/server/server.test.ts, e2e/proofs/t005-eperm-path-lock/
decision: —
notes: |
  Owner octopus. Tip origin/storylint/t005-path-lock @ 65af5ab. Holding land for falcon (session_iwazaru_…); not self-review.
  MECHANISM: load() ran OFF the per-instance store queue while saveDirect rename(tmp→dest) ran on it; GET /api/projects built ad-hoc ProjectStore per id (own queue). Windows EPERM when any handle open on dest during rename. Fix = path-keyed exclusive chain covering all load/save/update/switchFile across instances. No EPERM retry.
  SWALLOW ANSWER (de-escalates P0-data-loss → P1-reliability): failed rename → server 400; api.request throws; persistChapter setSaveState('error')+setError+rethrow; Shell role="alert" project-error; removeDraft only on success so local draft survives. Sheet saveSheet same setError path (leaveError is validation-only). Not silent vanish. Still top reliability item — blocks lands (6+ specimens).
  TWO SIGNATURES — do not collapse:
  (a) silent calm buffalo-incomplete: no stack — horse owns separately
  (b) this ticket: EPERM rename with stack @ helpers.mjs:1296
  Proof: BEFORE thrash 988 EPERM / open-handle 50/50; AFTER 0/0; MUTATION load unlocked+held FD → 11 EPERM then restore green.

---

## Context

Atomic project persistence uses temp file + rename. On Windows, rename can EPERM when another handle holds dest. Single-process: unqueued load races saveDirect. Not multi-agent-only.

Stack (canonical):

```
Error: ensureDraftReady normalize failed: 400 {"error":"EPERM: operation not permitted, rename
  '...\\data\\projects\\….json.tmp' -> '...\\data\\projects\\….json'"}
    at ensureDraftReady (e2e/helpers.mjs:1296)
```

## Acceptance detail

- [x] Rename-failure path tested (mutation + multi-instance thrash)
- [x] Author-visible error on failed Save (chapter + sheet setError → project-error alert)
- [x] No silent success after failed atomic rename
- [ ] Land + falcon APPROVE + koala author-facing verify
- [ ] Document AV/indexer as amplifier only (root is unqueued load)

## Repro

1. Isolated worktree @ origin/dev (pre-fix)
2. Loop calm/smoke until EPERM (~1/6–1/8) OR
3. `node --experimental-strip-types e2e/proofs/t005-eperm-path-lock/probe.mjs`
4. Mutation: unlock load() only + hold open FD → thrash unit fails EPERM
