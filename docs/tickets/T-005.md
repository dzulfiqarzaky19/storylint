---
id: T-005
title: Project atomic rename EPERM can fail saves on Windows
priority: P1
status: done
story: none
owner: octopus
branch: storylint/t005-path-lock
origin_dev_at_open: ea0500a
origin_dev_landed: df405c8
integrated_on: df405c8
landed_at: 2026-08-01
falcon: APPROVE @ f1d4a96 (session_iwazaru_…; MUT-1/2/6/7 + junction/cross-proc probes)
koala: PASS author-facing @ df405c8 — fail surfaces project-error; chapter+sheet save OK; probe thrash 0; MUT-6 swallow red
acceptance: Failed atomic project write (rename .json.tmp → .json) surfaces to the author; no silent loss; rename-fail path has a failing test
shortest_repro: calm-budget isolated loop run-8 @ ea0500a — ensureDraftReady create 400 EPERM rename under data/projects
l1_proof: e2e/proofs/t005-eperm-path-lock/ (BEFORE 988 EPERM thrash → AFTER 0; load-unlock mutation 11 EPERM; MUT-6 swallow red); server.test multi-instance + rename-fail reject
l2_required: no
blocks: []
blocked_by: []
files: src/server/store.ts, src/server/server.test.ts, e2e/proofs/t005-eperm-path-lock/
decision: —
notes: |
  DONE. Product origin/dev df405c8. Falcon APPROVE @ f1d4a96. Koala PASS author-facing.
  MECHANISM: load() ran OFF the per-instance store queue while saveDirect rename(tmp→dest) ran on it; GET /api/projects built ad-hoc ProjectStore per id (own queue). Windows EPERM when any handle open on dest during rename. Fix = path-keyed exclusive chain covering all load/save/update/switchFile across instances within one process. runLocked pins path at call time. No EPERM retry.
  SWALLOW ANSWER (P0-data-loss → P1-reliability): failed rename → server 400; api.request throws; persistChapter setSaveState('error')+setError+rethrow; Shell role="alert" project-error; removeDraft only on success so local draft survives. Sheet saveSheet same setError path. Not silent vanish.
  MUT-6 (falcon blocker, cleared): open FD on dest → save must reject EPERM + prior bytes remain. Swallow EPERM-only → that unit red (mut6-swallow-check.mjs).
  TWO SIGNATURES — do not collapse:
  (a) silent calm / T-006 — horse owns separately
  (b) this ticket: EPERM rename with stack @ helpers.mjs:1296
  Residual/follow-on (filed as T-007/T-008/T-009): junction path-key, per-process scope citation, rule 11a check. Not this land.

---

## Context

Atomic project persistence uses temp file + rename. On Windows, rename can EPERM when another handle holds dest. Single-process: unqueued load races saveDirect. Not multi-agent-only.

Stack (canonical):

```
Error: ensureDraftReady normalize failed: 400 {"error":"EPERM: operation not permitted, rename
  '...\\data\\projects\\….json.tmp' -> '...\\data\\projects\\….json'"}
    at ensureDraftReady (e2e/helpers.mjs:1296)
```

## Acceptance detail

- [x] Rename-failure path tested (mutation + multi-instance thrash + MUT-6 reject)
- [x] Author-visible error on failed Save (chapter + sheet setError → project-error alert)
- [x] No silent success after failed atomic rename (MUT-6)
- [x] Land + falcon APPROVE @ f1d4a96 → origin/dev df405c8
- [x] koala author-facing verify PASS @ df405c8
- [x] AV/indexer amplifier only; root is unqueued load (notes + proof README)

## Repro

1. Isolated worktree @ origin/dev (pre-fix)
2. Loop calm/smoke until EPERM (~1/6–1/8) OR
3. `node --experimental-strip-types e2e/proofs/t005-eperm-path-lock/probe.mjs`
4. Mutation: unlock load() only + hold open FD → thrash unit fails EPERM
5. MUT-6: `node e2e/proofs/t005-eperm-path-lock/mut6-swallow-check.mjs`
