---
id: T-005
title: Project atomic rename EPERM can fail saves on Windows
priority: P1
status: open
story: none
owner: unassigned
branch: —
origin_dev_at_open: ea0500a
acceptance: Failed atomic project write (rename .json.tmp → .json) surfaces to the author; no silent loss; rename-fail path has a failing test
shortest_repro: calm-budget isolated loop run-8 @ ea0500a — ensureDraftReady create 400 EPERM rename under data/projects
l1_proof: unit/integration covering rename failure path (not only happy-path atomic)
l2_required: no
blocks: []
blocked_by: []
files: []
decision: —
notes: |
  Found by buffalo during calm intermittent investigation (not the original silent specimen).
  TWO SIGNATURES — do not collapse:
  (a) silent specimen buffalo-calm-incomplete.output: visibility oks → no stack → exit 1 (cause open)
  (b) this ticket: EPERM rename throws with stack from ensureDraftReady / project create
  Isolated calm loop @ ea0500a: 7/8 PASS fingerprint eddd76a4; 1/8 this EPERM.
  Happy-path tests exist for atomic write; rename FAIL path unproven (rule 8a shape).
  If product Save can lose write without telling author → escalate P0 (rat).
---

## Context

Atomic project persistence uses temp file + rename. On Windows, rename can EPERM. Calm harness hit it mid-run and threw before HARD/FINGERPRINT markers — classifier sees stage-died-without-identity, but root is FS.

Stack (run-8, storylint-calm-crash @ ea0500a):

```
Error: ensureDraftReady create failed: 400 {"error":"EPERM: operation not permitted, rename
  '...\\data\\projects\\e2e-calm-....json.tmp' -> '...\\data\\projects\\e2e-calm-....json'"}
    at ensureDraftReady (e2e/helpers.mjs)
    at runViewport (e2e/calm-budget.mjs)
```

Not the original silent land specimen (no stack). Separate failure mode.

## Acceptance detail

- [ ] Rename-failure path tested (must fail red if swallowed)
- [ ] Author-visible error on failed project/sheet Save when underlying write fails
- [ ] No silent success after failed atomic rename
- [ ] Document whether Windows AV/indexer is a known contributor

## Repro

1. Isolated worktree @ origin/dev
2. Loop `node e2e/calm-budget.mjs` until create hits EPERM (observed ~1/8)
3. Or force rename fail in store layer test double
