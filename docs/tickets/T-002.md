---
id: T-002
title: Formalize L2 story/composition gate on origin/dev
priority: P1
status: integrated
story: agent-pipeline
owner: horse
branch: storylint/l2-composition-gate
origin_dev_at_open: 2e2561d
integrated_on: 850e4d0
acceptance: Documented + runnable L2 composition entry (story paths on origin/dev) distinct from L1 owned-stack; Verifier can fail story-close when L2 red
shortest_repro: npm run test:l2 on integrated origin/dev; must not equal all-smoke under a new name — shared-project Draft→Canon→Lab journey
l1_proof: guard-helpers + owned-stack refuse path (same as test:green)
l2_required: no
blocks: []
blocked_by: []
files: [e2e/l2-composition.mjs, package.json, docs/AGENT_PIPELINE.md, e2e/README.md, e2e/proofs/T-002-l2-composition-mutation.txt, e2e/proofs/T-002-l2-reverse-draft-clobber-mutation.txt]
decision: docs/AGENT_PIPELINE.md
notes: |
  Process ticket. L2 is composition, not more isolation.
  Gap answer: all-smoke isolates each smoke on its own mint; L2 keeps one project across surfaces.
---

## Context

Founder flow pools work on dev until a story completes, then runs composition E2E. L1 (`test:green` / all-smoke) is isolation-shaped. A rule with no runner is a rule nobody follows.

## Gap (what L2 checks that all-smoke does not)

- **all-smoke:** each feature smoke mints a private project; suite asserts post-smoke active ∈ that mint. Sibling contamination is caught; multi-surface shared state is not exercised.
- **L2:** one project P; Draft body on P; Canon sheet seed must not blank that body (forward checkpoint); Draft chapter rewrite after seed must not wipe sheets (reverse); Lab+reload; prove post-seed body + sheet + active still P.

## Acceptance detail

- [x] Named L2 command: `npm run test:l2`
- [x] Distinct from L1 (shared-container journey in `e2e/l2-composition.mjs`)
- [x] Evidence discipline matches test:green (owned stack, provenance, fail-closed)
- [x] Docs: AGENT_PIPELINE + e2e/README
- [x] Land + self-pass on integrated tip (this ticket is the gate itself; `l2_required: no`)
- [x] Fail path doctrine: reopen ≥ prior priority with shortest repro (AGENT_PIPELINE)

## Integrated

origin/dev=`850e4d0` — dual-direction L2 close land.
- Gate: `npm run test:l2` shared-project Draft→Canon→Lab with forward body checkpoint after sheet seed + reverse draft-after-seed.
- Hawk tigress APPROVE @ `6aa3621` (ancestor of land tip; both mutation directions independently red).
- Self-pass L2 on topic-after-dev and land bubble ancestry.
- Forward proof: e2e/proofs/T-002-l2-composition-mutation.txt
- Reverse proof: e2e/proofs/T-002-l2-reverse-draft-clobber-mutation.txt
- First gate land was `f48ef3d`; this tip closes the reverse-direction gap without losing forward coverage.

## Mutation proof

Both directions must red on the same tip (hawk REQUEST CHANGES @ d9a2e48: reassigning chapterBody after post-seed write repaired forward damage).

- Forward (Canon->Draft): e2e/proofs/T-002-l2-composition-mutation.txt — sheet PUT blanks chapter bodies -> test:l2 exit=1 `draft body lost after Canon sheet seed` (checkpoint before post-seed Draft rewrite). load_bearing_red=YES. Product tree restored.
- Reverse (Draft->Canon): e2e/proofs/T-002-l2-reverse-draft-clobber-mutation.txt — chapter PUT wipes sheets; draft-after-seed distinct body -> test:l2 exit=1 sheet missing after post-seed Draft write. load_bearing_red=YES. Product tree restored.

## Named claim (not general composition-safe)

`test:l2` proves **two specific directions** on one shared project:

1. A Canon sheet PUT cannot blank Draft chapter bodies (forward checkpoint at seed).
2. A Draft chapter PUT cannot wipe or rename Canon sheets (reverse after post-seed write).

Lab is on the journey (switch + reload still share container P) but is **not** under a clobber mutation assertion. Inbox and graph are not claimed. Do not cite L2 as "no surface can clobber another" in general — only the two directions above, plus body/sheet/active survival across the Draft→Canon→Lab→reload path.
