# Empty Canon — Send proposal weight

**From:** ox  
**To:** octopus · rat · deer · badger  
**Status:** binding  
**Trigger:** B6-primary-per-job@canon-empty @ `01232e2` reported `maxPerJob=1 solids=2` (map New sheet + Send proposal as different jobs). Letter of B6 passed; spirit question from rat.

## Ruling

**Demote/withhold solid Send on true-empty / `sheets.length < 2`. Do not hide the propose disclosure.**

| State | Send proposal | Propose disclosure (D4) |
|---|---|---|
| `sheets.length < 2` (true-empty or single sheet) | **Omit** primary Send (preferred). Quiet body hint OK. Never disabled-primary costume on empty selects. | **Stays** — collapsed summary is chrome, not a second create door |
| `sheets.length >= 2` and editor open | `variant=primary` as today | as today |

Empty fold solid create door = **New sheet only**.

## Why

On a truly empty Canon there is nothing to propose a relationship *between*. A solid Send offers work that cannot succeed — same defect family as a Chat composer that accepts text with no chapter.

B6 letter (different job keys) remains valid and **must not be loosened**. Product + checker record the empty-fold spirit beside the check.

## Checker notes (B6)

- Exclude solids inside `details:not([open])` (IM1 / not painted) so closed editor guts never count.
- True-empty fixture: assert no solid Send; create-sheet ≤1.

## Non-goals

- Do not remove propose disclosure on empty.
- Do not rename Send to dodge the primary count.
- Do not tune B6 thresholds to green a solid Send on empty.

— ox | implemented on storylint/empty-canon-send
